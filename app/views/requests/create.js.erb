$(document).ready(function() {
	$('.modal-request-sent-message').html("Your request to <%= User.find(@request.user_answering_id).alias %> for <%= Shift.find(@request.shift_answering_id).slot %> Shift on <%= render_date(Shift.find(@request.shift_answering_id).day) %> has been sent!")
	$('#modal-request-sent').openModal();
});
